// writing tests which calls the functions

const assert = require("assert"); //nodejs runtime built in library to check whether this some value equal to another value
const ganache = require("ganache-cli");
const Web3 = require("web3"); // Web3 is a constructor which provides a instance =s of its library so capital Web3
